from flask_sqlalchemy import SQLAlchemy

# Crea la instancia de SQLAlchemy aquí, sin asociarla a ninguna aplicación todavía.
# Esta será la única instancia de 'db' que usará toda tu aplicación.
db = SQLAlchemy()
